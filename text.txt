  //This wil display the selected items in the cart list
    // const orderedList = document.createElement('li');
    // orderedList.textContent = `${foodName} - ${foodPrice}`
    // console.log(orderedList)
    // cart.appendChild(orderedList)

    //and this will update the prices
    // total += price
    // totalPrice.textContent = 'N' + total


    *******cut out from line 32***


    /* .logo-section{
  display: flex;
  align-items: center;
  /* justify-content: space-evenly; */
  position: sticky;
  top:0;
  z-index: 1000;
  width: 100%;
  height: 50px;
} 